# Build artifacts and fetched deps — regenerated by `mix deps.get` / `mix compile`.
/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/

# Local secrets for docker compose (POSTGRES_PASSWORD, SECRET_KEY_BASE, SITE_ADDRESS). Never commit.
.env
